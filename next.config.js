/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'upload.wikimedia.org',
      'www.fast.ai',
      'www.gstatic.com',
      'huggingface.co'
    ],
  },
};

module.exports = nextConfig; 